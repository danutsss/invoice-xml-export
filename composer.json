{
	"description": "This is a simple UCRM plugins that will export all invoices (based on selected org. & dates) to a XML file.",
	"license": "MIT",
	"version": "1.0.1",
	"autoload": {
		"psr-4": {
			"App\\": [
				"classes/"
			]
		}
	},
	"require": {
		"league/csv": "^9.1",
		"ubnt/ucrm-plugin-sdk": "~0.5",
		"twig/twig": "^3.5",
		"vlucas/phpdotenv": "^5.5"
	},
	"config": {
		"platform": {
			"php": "7.3"
		}
	}
}
